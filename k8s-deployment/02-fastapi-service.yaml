apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: monitoring-demo
  labels:
    app: fastapi-app
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
  selector:
    app: fastapi-app
